import { DateFormatCostomDirective } from './date-format-costom.directive';

describe('DateFormatCostomDirective', () => {
  it('should create an instance', () => {
    const directive = new DateFormatCostomDirective();
    expect(directive).toBeTruthy();
  });
});
